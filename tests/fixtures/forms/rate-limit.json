{
  "easyForm": { "type": "form", "version": 1 },
  "form": {
    "name": "E2E Rate Limit",
    "handle": "e2eRateLimit",
    "description": "maxSubmissionsPerUser = 1, used to verify the per-user submission cap (logged-in users).",
    "fieldLayout": {
      "schemaVersion": 3,
      "extraFieldPolicy": "allowListed",
      "pages": [
        {
          "id": "page_1",
          "label": "Form",
          "rows": [
            {
              "id": "r1",
              "stackOnMobile": true,
              "fields": [
                { "id": "f1", "type": "text", "label": "Name", "handle": "name", "required": true }
              ]
            }
          ]
        }
      ],
      "frontendFields": [],
      "promotedFields": { "primaryName": "name" }
    },
    "enabled": true,
    "captchaProvider": null,
    "maxSubmissionsPerUser": 1
  }
}
