{
  "easyForm": { "type": "form", "version": 1 },
  "form": {
    "name": "E2E Webhook",
    "handle": "e2eWebhook",
    "description": "Posts each submission to the local test webhook sink (e2e).",
    "webhookUrl": "http://localhost/webhook-sink.php",
    "webhookPayload": "full",
    "fieldLayout": {
      "schemaVersion": 3,
      "extraFieldPolicy": "allowListed",
      "pages": [
        {
          "id": "page_1",
          "label": "Page 1",
          "rows": [
            {
              "id": "row_1",
              "stackOnMobile": true,
              "fields": [
                { "id": "f1", "type": "text", "label": "Name", "handle": "name", "required": true },
                { "id": "f2", "type": "email", "label": "Email", "handle": "email", "required": true }
              ]
            }
          ]
        }
      ],
      "frontendFields": [
        { "handle": "utm_source", "label": "UTM Source", "type": "string" }
      ],
      "promotedFields": { "primaryEmail": "email", "primaryName": "name" }
    },
    "enabled": true,
    "captchaProvider": null
  }
}
